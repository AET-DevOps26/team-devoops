settings.json
AGENTS.md
debug.log
web-client/.env.development
services/spring-letter/.env

# Local env files
.env
.env.development
.env.*.local

# Agent instructions (local, not shared)
AGENTS.md

# Build-tool binaries — provided via the build's dependency management, never committed
lombok-*.jar
